so for move generation i will have to first get pseudolegal moves
then i remove illegal ones
then i return the list i guess?

now python is notorious with using the original objects and not their copies
so first study that

i have this structure -> position { board { grid, notation } }
i wanna manipulate the grid and the notation
so create copies of notations, replace an underscore at a time with a stone {1, 2} and construct a board object from that notation
append, repeat, and return

sorting and searching for which move first is a different thing altogether, not my job over here
or shall i do that

okay so first check which moves shall be given priority
like checks captures in chess, what in go? atari? ko? non ko?






okay, the copy thing is sorted
just make a new board

i am deleting notation as an attribute
managing two things aint worth it
rather def get_notation(position : Position) -> str:










fuck everything
new start
here are 7 criteria, based on which i will order a move
    1. capturing moves
    2. moves that put opponent in atari
    3. moves that save your group from atari
    4. connection moves
    5. cutting moves
    6. neutral moves
    7. self-atari moves (last / prune)
i will use a numpy.uint16 to represent a move
move will be a separate class of its own
it will support comparisons as well, since i want to use default python sorting function for the move list as writing it in python would result slow performance compared to cpython, lesgo
so out of the 16 bits, the 7 MSBs will have those 7 criteria; MSB represents 1, MSB - 1 represents 2, and so on till MSB - 6
there onwards, 9 bits are left and i will use them to store the move squares
2^9 is 512 and therefore i can store 512 different moves (unsigned)
i will have 362 moves at maximum (19 * 19 + pass)
so all good, 16 bits properly utilised
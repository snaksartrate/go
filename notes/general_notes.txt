black plays first lmao

environment -> a board / a position, number of stones each player has, number of captures made by each players
move_gen -> place a stone, pass

so the agent function gets the environment as the input and returns a value from the move_gen function as the output

train using rl (...sure? ...or mcts?)

heuristic is going to be
number of squares covered, number of captures, score
score will come into play once both players have passed

use minimax,

use dfs
in bfs, limit to a beam width of n, where n = number of intersections on an edge, choose best n options










so i have a board ready
i have moved notation out of the way, and definied a funciton to get notation whenever i want
now i suppose i shall use numpy arrays to represent board grids since i wanna send those as inputs
...also, shall i put feature analysis?








for heuristic function, i will use territories covered, liberties, captures
priority moves to be checked will be _______? (maybe atari n captures n stuff)




# 🏗️ Final Architecture (THIS is your project)

```text
Go Engine Project

1. Game State
   - Board
   - Rules

2. Move Generator

3. Heuristic Evaluation

4. Agents:
   A. Minimax Agent (with alpha-beta)
   B. MCTS Agent (main)

5. (Optional)
   - Beam Search Agent
   - RL improvement
```
